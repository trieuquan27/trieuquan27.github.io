# This is a basic workflow that is automatically triggered when push on master branch

name: Dev Workflow

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
<<<<<<< HEAD
  trigger-test:
    runs-on: ubuntu-latest

    steps:
      - name: Dispatch QA Workflow
        id: dispatch_qa_workflow
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.TEST_REPO_TOKEN }}
          repository: trieuquan27/test.github.io
          event-type: qa_workflow
  # get-event-id:
  #   name: Get Event ID
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Get Event ID
  #       id: get_event_id
  #       run: echo "::set-output name=event_id::${{ github.event.client_payload.id }}"
        
  # check-external-job:
  #   name: Check External Job Status
  #   runs-on: ubuntu-latest
  #   needs: [trigger-test]
  #   if: github.event_name == 'workflow_dispatch' || github.event_name == 'repository_dispatch'

  #   steps:
  #     - name: Check External Job Status
  #       id: check_external_job
  #       uses: actions/github-script@v4
  #       with:
  #         github-token: ${{ secrets.GITHUB_TOKEN }}
  #         script: |
  #           const { data: runs } = await github.actions.listWorkflowRuns({
  #             owner: 'trieuquan27',
  #             repo: 'test',
  #             workflow_id: '8869057882',
  #             branch: 'master',
  #             event: 'workflow_dispatch',
  #             per_page: 1
  #           });
  #           const lastRun = runs.workflow_runs[0];
  #           if (lastRun.conclusion === 'failure') {
  #             core.setFailed('External job failed');
  #           }

  # publish:
  #   name: Publish the package
  #   runs-on: ubuntu-latest
  #   needs: [trigger-test, check-external-job]
  #   if: github.event_name == 'workflow_dispatch' && github.event.action == 'completed' && needs.check-external-job.outputs.check_external_job != 'failure'
=======
  # trigger-test:
  #   runs-on: ubuntu-latest
>>>>>>> b13fd3c44f9eebb82c8fa02db7e7ac21b798f606

  #   steps:
  #     - name: Dispatch QA Workflow
  #       id: dispatch_qa_workflow
  #       uses: peter-evans/repository-dispatch@v2
  #       with:
  #         token: ${{ secrets.TEST_REPO_TOKEN }}
  #         repository: trieuquan27/test.github.io
  #         event-type: qa_workflow
  trigger-test:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch an action and get the run ID
        uses: codex-/return-dispatch@v1.13.0
        id: return_dispatch
        with:
          token: ${{ secrets.TEST_REPO_TOKEN }}
          ref: master
          repo: test
          owner: trieuquan27
          workflow: playwright.yml
      - name: Await Run ID ${{ steps.return_dispatch.outputs.run_id }}
        uses: Codex-/await-remote-run@v1.12.2
        with:
          token: ${{ secrets.TEST_REPO_TOKEN }}
          repo: test
          owner: trieuquan27
          run_id: ${{ steps.return_dispatch.outputs.run_id }}
          run_timeout_seconds: 300 # Optional
          poll_interval_ms: 5000 # Optional..
